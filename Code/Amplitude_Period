import numpy as np

time,mag,err = np.loadtxt('/home/parallels/photometry_3199.dat.txt.varformat.txt',unpack=True)

print(mag)
magLength = len(mag)
maxMag = amax(mag)
#sort to find max and second max
magSorted = mag.sort()
max1 = magSorted[0]
max2 = magSorted[1]
maxAmp = np.mean(max1,max2)
max1Index = where(mag==max1)
max2Index = where(mag==max2)

timeMax1Index = timeMax1[max1Index]
timeMax2Index = timeMax2[max2Index]

maxTime = np.mean(timeMax1Index,timeMax2Index)

print(time[time1Index])

min1 = minSorted[-1]
min2 = minSorted[-2]
minAmp = np.mean(min1,min2)
min1Index = where(mag==min1)
min2Index = where(mag==min2)

timeMin1Index = timeMin1[min1Index]
timeMin2Index = timeMin2[min2Index]

minTime = np.mean(timeMin1Index,timeMin2Index)


Amplitude = maxAmp - minAmp

Period = maxTime - minTime


print('Amplitude: ', Amplitude)
print('Period: ', Period)
